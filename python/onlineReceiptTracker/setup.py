from setuptools import setup

setup(
        name='onlineReceiptTracker',
        packages=['onlineReceiptTracker'],
        include_package_data=True,
        install_requires=[
            'flask',
        ]
    )
